import { StyleSheet } from "react-native";

export default GetCreateFd_OTPScreenStyles = (theme) => {
  return StyleSheet.create({
    middleView: {
      flex: 1,
      justifyContent: "center",
      alignItems: "center",
      margin: 20,
    },
  });
};
